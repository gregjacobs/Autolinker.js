name: Run tests on PR

on:
  push:
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Yarn install
        run: yarn

      - name: Run tests
        run: yarn test
