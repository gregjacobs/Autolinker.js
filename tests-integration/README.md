The files in this folder will be copied to a `.tmp` folder when `yarn test` is
executed.

From there, it will install the packed version of Autolinker, and check that the
package contents export files correctly.