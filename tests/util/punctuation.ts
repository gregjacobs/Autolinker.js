/**
 * Used for unit tests, the punctuation characters to test at the end of a
 * match (including empty string for no punctuation).
 */
export const punctuationChars = ['', '?', '!', ':', ',', '.', ';'];
